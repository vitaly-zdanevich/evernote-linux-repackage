{
  "name": "evernote-linux-repackage",
  "version": "0.1.0",
  "private": true,
  "description": "Repackage the official Evernote Electron desktop client for Linux without Wine.",
  "scripts": {
    "build": "node scripts/build-linux-port.js",
    "check": "npm run format:check && node --check scripts/build-linux-port.js && node --check scripts/check-media-support.js && node --check scripts/doctor.js && node --check scripts/patch-evernote-bundle.js && node --check scripts/package-appimage.js && node --check scripts/smoke-test.js && node --check scripts/start-built.js && node --check scripts/verify-artifact.js && node --check scripts/write-checksums.js && bash -n scripts/smoke-test-clean-container.sh && sh -n scripts/templates/evernote-launcher.sh && sh -n scripts/templates/appimage-AppRun.sh",
    "checksums": "node scripts/write-checksums.js",
    "check:media": "node scripts/check-media-support.js",
    "doctor": "node scripts/doctor.js",
    "format": "prettier --write \"{scripts,test}/**/*.js\" package.json .prettierrc.json",
    "format:check": "prettier --check \"{scripts,test}/**/*.js\" package.json .prettierrc.json",
    "package:appimage": "node scripts/package-appimage.js",
    "smoke": "node scripts/smoke-test.js",
    "test": "node --test test/*.test.js",
    "verify": "node scripts/verify-artifact.js",
    "start": "node scripts/start-built.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=24",
    "npm": ">=11"
  },
  "devDependencies": {
    "prettier": "3.8.3"
  }
}
